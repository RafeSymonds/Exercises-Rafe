# import what you need

import math
import matplotlib.pyplot as plt
import matplotlib.image as mpimg
import numpy as np
import random

# should return an image by using plt.imread
def returnImage(fileName: str):
    return None

# should return a 10 by 10 grid of integers
# 1 = star
# 0 = blank
def generateSolution(regions: np.array) -> np.array:
    return None

# checks to see if the solution matches with the grid
def validateSolution(regions: np.array, solution: np.array) -> bool:
    return None

# should return a 10 by 10 grid
# value corresponds to region
def generateRegionsFromImage(img) -> np.array: # image processed from imread
    return None
